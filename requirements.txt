Flask==3.0.3
Flask-Cors==4.0.0
pymongo==4.6.3
dnspython==2.6.1        # Needed for MongoDB Atlas connection
Werkzeug==3.0.3
python-dotenv==1.0.1    # Optional (useful for local .env)
itsdangerous==2.1.2
Jinja2==3.1.4

# Email / MIME support (usually included with Python, added for safety)
email-validator==2.1.1

# Optional (if you plan to use Mongo indexes or improvements)
pytz==2024.1

# For production safety (optional but recommended)
gunicorn==22.0.0
